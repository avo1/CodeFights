// https://codefights.com/challenge/M8cR5yNub3PrwXkkp
// 1000

let betaExor = {
    //Int(($0 as String).utf8.reduce(0, combine: ^))
    //$0.utf8.map{Int($0)}.reduce(0, combine: ^)
    Int($0.utf8.reduce(0, combine: ^))
}
